node_modules/
test-results/
playwright-report/
.vscode/mcp.json
.idea
.DS_Store
.env
sessions/
.playwright-mcp/

# Vendored playwright monorepo (shallow clone at pinned commit).
# Source is edited directly; lib/ is built locally by `npm run build:pw`.
playwright-src/
